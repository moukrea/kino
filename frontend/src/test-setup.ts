// Test setup file required by `vitest.config.ts`. Currently a no-op; jsdom is
// configured globally via the vitest config so individual tests don't need to
// repeat the boilerplate. Real test helpers (e.g. SolidJS testing-library
// wiring) land alongside the features that need them.
export {};
